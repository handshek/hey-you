[project]
name = "heyyou-agent"
version = "0.1.0"
description = "HeyYou Vision Agent — AI greeter for physical spaces"
requires-python = ">=3.11"
dependencies = [
    "vision-agents",
    "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"
